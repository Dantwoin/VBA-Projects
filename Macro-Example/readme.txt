
This project is an example of macros used to filter data in a graph. It functions similar to the Splicers Example, but can be uses the record macro tool to accomplish the goal. I then editted the VBA code to optimize the process. 
